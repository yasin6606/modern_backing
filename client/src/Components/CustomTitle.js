const CustomTitle = ({title}) => {
    return <>
        <div>
            <p className="text-4xl font-extrabold text-gray-900 tracking-wider leading-snug border-b-2 border-blue-200 pb-3 text-center">{title}</p>
        </div>
    </>
}

export default CustomTitle;
